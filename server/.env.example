# Required, no default — the server does not start without these.
# IANA zone id the times in Telegram messages are rendered in.
DISPLAY_ZONE=
DATABASE_PASSWORD=

# Fall back to empty when unset.
DATABASE_URL=
DATABASE_USERNAME=
TELEGRAM_BOT_TOKEN=
